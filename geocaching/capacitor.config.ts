import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'geocaching',
  webDir: 'dist/geocaching',
  bundledWebRuntime: false
};

export default config;
